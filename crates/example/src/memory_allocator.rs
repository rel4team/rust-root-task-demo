
const START_ADDR: usize = 0x200_0000;

pub struct MemoryAllocator {
    req_num: usize,
    current: usize,
    recycled: Vec<usize>

}

impl MemoryAllocator {

    // pub fn new() -> Self {

    // }

    pub fn run_memory_alloc_test() {
        
    }

}